import 'package:flutter/material.dart';

class JSATabContent extends StatelessWidget {
  const JSATabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'JSA Tab Content - Coming Soon',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
