import 'package:flutter/material.dart';

class BackupTab extends StatelessWidget {
  const BackupTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Backup Tab Content'),
    );
  }
}
