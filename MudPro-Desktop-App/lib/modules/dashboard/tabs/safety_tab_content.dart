import 'package:flutter/material.dart';

class SafetyTabContent extends StatelessWidget {
  const SafetyTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Safety Tab Content - Coming Soon',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
